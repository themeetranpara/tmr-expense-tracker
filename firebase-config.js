// ============================================================
// TMR Expense Tracker — Firebase Configuration
// ============================================================
// Paste the config object from:
// Firebase Console → Project Settings → General → "Your apps" → SDK setup and configuration
//
// Replace EVERY value below — the placeholders will not work.
// This file only holds configuration. No Firebase logic lives here
// (see firebase-init.js for that) so you never have to touch app
// logic just to update your project's keys.
// ============================================================

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
